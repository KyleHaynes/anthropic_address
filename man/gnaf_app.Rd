% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{gnaf_app}
\alias{gnaf_app}
\title{Launch an interactive geocoding app for GNAF matching}
\usage{
gnaf_app(
  con = NULL,
  db_path = NULL,
  launch.browser = interactive(),
  run = TRUE
)
}
\arguments{
\item{con}{Optional DBI connection created by [gnaf_connect()]. If omitted,
the app can open a connection from `db_path`.}

\item{db_path}{Optional DuckDB path to pre-fill in the app and connect to on
launch when `con` is `NULL`.}

\item{launch.browser}{Passed to [shiny::runApp()] when `run = TRUE`.}

\item{run}{If `TRUE` (default), launches the app immediately. If `FALSE`,
returns the [shiny::shiny.appobj()] for embedding or testing.}
}
\value{
Invisibly returns the app object when `run = TRUE`, otherwise returns
  the app object.
}
\description{
Opens a Shiny app that lets you connect to a DuckDB database, submit one
address per line, review `gnaf_match()` results, and inspect full-string
Jaro-Winkler and Jaccard similarity diagnostics between the input string and
the matched address label.
}
