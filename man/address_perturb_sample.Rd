% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{address_perturb_sample}
\alias{address_perturb_sample}
\title{Sample and perturb address labels for bulk-match testing}
\usage{
address_perturb_sample(
  x,
  n = min(1000L, nrow(x)),
  replace = FALSE,
  seed = NULL,
  max_changes = 2L,
  keep_original = TRUE
)
}
\arguments{
\item{x}{A `data.table` or `data.frame` containing either `ADDRESS_LABEL` or
`address_label`.}

\item{n}{Number of rows to sample. Defaults to `min(1000L, nrow(x))`.}

\item{replace}{Sample with replacement. Default `FALSE`.}

\item{seed}{Optional random seed for reproducible output.}

\item{max_changes}{Maximum number of perturbations to apply per address.
Default `2L`.}

\item{keep_original}{If `TRUE` (default), includes the original label in the
output.}
}
\value{
A `data.table` containing sampled rows plus a `simulated_address`
  column and a `perturbations` column describing what changed.
}
\description{
Draws a sample of addresses from a GNAF-like `data.table` and applies small,
realistic string perturbations to the address label so you can test fuzzy and
bulk matching on messier inputs.
}
\details{
Supported perturbations include abbreviation swaps (`ROAD` -> `RD`), comma
removal, case changes, whitespace noise, dropped unit/building prefixes, and
small single-character typos.
}
