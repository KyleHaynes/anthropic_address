% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_helpers.R
\name{plot_boundaries_heatmap}
\alias{plot_boundaries_heatmap}
\title{Plot polygon boundaries and a heatmap of latitude/longitude points}
\usage{
plot_boundaries_heatmap(
  shapes,
  points_dt = NULL,
  lat = "latitude",
  lon = "longitude",
  simplify_tolerance = NULL,
  bins = 150,
  alpha = 0.6,
  palette = viridisLite::viridis,
  verbose = TRUE,
  use_leaflet = FALSE,
  heatmap_options = list()
)
}
\arguments{
\item{shapes}{An `sf` polygon object.}

\item{points_dt}{Optional data.frame / data.table of points with latitude/longitude.}

\item{lat}{Name of latitude column in `points_dt`.}

\item{lon}{Name of longitude column in `points_dt`.}

\item{simplify_tolerance}{If provided (numeric), geometries are simplified with this tolerance.}

\item{bins}{Number of grid cells for density estimation (higher = finer).}

\item{alpha}{Alpha for the density raster.}

\item{palette}{Color palette function (defaults to `viridisLite::viridis`).}

\item{use_leaflet}{If `TRUE`, render an interactive `leaflet` map using `leaflet.extras::addHeatmap`.}

\item{heatmap_options}{A named list of options passed to the leaflet heatmap (e.g. `radius`, `blur`, `max`, `minOpacity`).}
}
\value{
A `ggplot` object (when `use_leaflet = FALSE`) or a `leaflet` map object (when `use_leaflet = TRUE`).
}
\description{
Draws polygon boundaries (optionally simplified) and overlays a 2D density
heatmap of the provided points. Uses `ggplot2` + `sf` for fast, static
plotting. For very large point sets, consider pre-aggregating or using
a smaller `chunk_size` when performing lookups.
}
